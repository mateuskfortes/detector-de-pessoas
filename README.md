# detector de pessoas

## como rodar

1. Instale as dependências:

```shell
pip install -r requirements.txt
```

2. Dentro da pasta server, rode o servidor com https:

```shell
python manage.py runserver_plus 0.0.0.0:80 --cert-file cert.crt
```
